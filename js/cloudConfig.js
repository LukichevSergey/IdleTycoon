/**
 * Настройки облачных сохранений.
 *
 * Заполните два поля из консоли Firebase (Настройки проекта -> Общие):
 *   apiKey    — «Веб-ключ API» (Web API Key);
 *   projectId — «Идентификатор проекта» (Project ID).
 * Пока поля пустые, раздел «Облако» в игре показывает инструкцию
 * и остаётся выключенным — на остальную игру это никак не влияет.
 *
 * Эти значения НЕ являются секретом: веб-ключ Firebase публичен по замыслу,
 * его видно в любом клиенте. Доступ к чужим сейвам закрывают правила
 * Firestore (каждый читает и пишет только документ со своим uid),
 * а не скрытность ключа. Коммитить их в открытый репозиторий безопасно.
 */
const CLOUD_CONFIG = {
  apiKey: "AIzaSyCnSXOvKrgjIleI4c1M7_U1sKpnpx_02t0",
  projectId: "idle-finance-tycoon",
};
